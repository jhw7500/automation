# Handoff — automation

_2026-09-05 · claude-code · main = d005a40 · **v1.69 릴리스 완료** (v1.68 은 태그만, 롤아웃 불가로 v1.69 로 직행)_

## 체크포인트 — v1.69 완료, 다음 작업 대기
- **완료·검증됨**: 태그 `v1.69`(tag object `9c62be4b…` → commit `31e1da1b`), 플릿 17/17,
  기본 `automation_ref` = v1.69(범프 PR #149 머지, `d005a40`). 마지막 감사 `--ref` 없이
  `total=17 current=17 drift=0 blocked=0`. 폐기 실측 확인 — 소비자 4곳에서 `gemini-pr-review.yml` 404.
- **v1.68 은 태그만 남는다** — 그 시점 도구로는 롤아웃이 17/17 BLOCKED 였고 태그는 이동 불가.
  내용은 v1.69 에 모두 포함된다.
- **다음 액션 1개**: 열린 이슈 중 선택 — **#145**(OpenCode 진단 가시성) · #133(잔여: `gemini-review.yml:378`
  금지 문장 + 중앙 파일 정리) · #128 · #125 · #106 · #93 · #83.
- **제약**: `gh`·`git push` 모두 `env -u GITHUB_TOKEN`. 롤아웃은 `--actionlint /tmp/actionlint-v1.7.12/actionlint`.
  actionlint 는 CI 플래그(`-shellcheck= -pyflakes=`)로만. 라벨은 `opened` 실행 종료 후.
  **호스트 메모리 빠듯 — BG 작업이 이 세션에서 6회 강제종료됐다.** 전체 스위트는 3분할 권장
  (`--ignore=tests/test_verify_workflow_release.py --ignore=tests/test_review_workflow_logic.py` + 각각 단독).

## 이번 사이클에서 확인된 사실 (재사용할 것)
- **폐기·삭제를 처음 하는 경로는 스위트 통과로 검증되지 않는다** — 배포된 상태에서 폐기한 적이 없어
  "소비자가 폐기 캘러를 아직 갖고 있는" 픽스처가 없었고, 3084건이 통과하는 동안 실제 plan 은 17/17 차단됐다.
  **반드시 실제 plan 을 돌려 본다.** 이 세션에서 같은 형태를 세 번 만났다(#133 계약 부재,
  v1.67 릴리스 게이트 부재, 이번 롤아웃).
- **정본 트리는 카탈로그와 정확히 일치해야 한다** — 파일을 남기고 `retired` 만 표시할 수 없다.
  파일·카탈로그 항목·설정 키 셋을 함께 지우고, 복원 헬퍼도 셋을 함께 되돌린다(서로를 검사한다).
- **OpenCode 실패 재현에는 새 head 가 필요하다** — `gh run rerun --failed` 는 모델을 다시 부르지 않고
  (모델 잡은 실패를 출력으로 알리며 잡 수준에서는 success), 무플래그 전체 재실행은 예산이
  `duplicate_head` 로 거부한다. 재실행 반복을 독립 시도로 세면 한 번의 실패가 여러 번으로 보인다.
- **`candidate_contract_failed` 는 계약 위반이 아닐 수 있다** — 후보 추출 실패에도 같은 이름이 붙는다.
  판별은 후보 아티팩트의 `candidate_validations`(빈 배열이면 검사 미실행)와 `review_sha256`(null).

## v1.68 / v1.69 내용
- **#133 잔여**: `@gemini-cli /review` 뒤 텍스트가 이제 모델에 닿는다. diff 와 같이 파일로 넘기고
  프롬프트는 파일명만 지칭. 답글은 부모 코멘트 본문을 싣는데 그 작성자는 권한 검사를 거치지 않으므로
  UNTRUSTED 표시가 실질 근거를 갖는다.
- **#143**: `workflow_dispatch` PR 리뷰 폐기. 자기 `PATH` 재정의가 액션의 출력 임시파일 읽기를 막아
  2026-01-22 이후 성공 0건이었고, `pr_number` 를 받고도 범위·체크아웃에 쓰지 않았다. 13건 vs 3,154건.
- **롤아웃 수정(v1.69)**: 폐기 캘러를 "선언된 적 없는 캘러" 로 읽지 않고 계획된 삭제로 넘긴다.

## (이전) v1.67 · v1.66 · v1.65 — 완료
- v1.67: 태그 `88a2a115…` → `fbcbb30b`, 범프 PR #142. 수동 `/review` 에 diff 공급 + 구조 계약.
- v1.66: 태그 `bfdff11b…` → `14f97fb4`, 범프 PR #139. 라벨 불일치를 거절로 + opencode 문구 동등화.
- v1.65: 태그 `35379e75…` → `3bd49095`, 범프 PR #137. skip 사유 오귀속 수정.
